scalar Time

schema {
  query: Query
  # mutation: Mutation
}

type User {
  id: ID!
  idpId: String!
  email: String!
  created: Time!
  createdBy: String!
  modified: Time!
  modifiedBy: String!
}

type Query {
  userInfo: User!
}
